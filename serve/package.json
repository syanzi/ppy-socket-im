{
  "name": "socket-serve",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "egg-bin dev --cluster --sticky",
    "start": "egg-scripts start --daemon --workers=2 --ignore-stderr",
    "stop": "egg-scripts stop"
  },
  "dependencies": {
    "egg": "^2.0.0",
    "egg-mysql": "^3.0.0",
    "egg-redis": "^2.4.0",
    "egg-scripts": "^2.13.0",
    "egg-socket.io": "^4.0.0"
  },
  "devDependencies": {
    "egg-bin": "^4.3.6"
  }
}
